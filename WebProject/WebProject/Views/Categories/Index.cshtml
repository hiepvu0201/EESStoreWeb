@model IEnumerable<WebProject.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tenDM)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tongSoLuongSP)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tenDM)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tongSoLuongSP)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.maDM">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.maDM">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.maDM">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
